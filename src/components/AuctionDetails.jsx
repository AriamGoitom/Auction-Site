const AuctionDetails = () => {     /* This defines a functional component named AuctionDetails. */
return (                            /* Inside the AuctionDetails component, it returns JSX. In this case, it's a fragment (<>...</>) containing the text "Hej Ariam!". Fragments are a way to group multiple elements under a single parent without adding unnecessary nodes to the DOM. */
    <>Hej Ariam!</>
)
}
export default AuctionDetails      /* This exports the AuctionDetails component, making it available for import in other files. */